13:
    1.简化了字符串显示在屏幕上的操作(填充背景色、画字符、刷新图层), 合并到一个函数了;
    2.调整了数据缓冲区FIFO8=>FIFO32, 将所有鼠标键盘定时器中断数据合并到同一个缓冲区
        , 通过数据内容区分数据类型, 具体看FIFO缓冲区中断类型表;
    3.新增了测试系统性能的功能, 主要原理是从测试启动系统3s后开始, 使cpu全力做累加
        , 直到10s, 观察这7s内累加的值(越大越优), 根据相对值推断操作系统性能;
    4.终于完善了定时器管理的部分, 现在用链表对定时器做管理, 不用对已为超时的定时器做位移操作了,
        同时增加了"哨兵"定时器(超时时间为0xffffffff, 理论上不可达)
        , 使得定时器链表永远至少有一个节点, 节约了插入时的判断开销;
14:
    5.提高了显示画面的分辨率, 并做了VBE检查, 增强了显卡的兼容性
    6.做了一个textbox一样的东西, 终于可以把键盘输入的内容显示在画面上了
    