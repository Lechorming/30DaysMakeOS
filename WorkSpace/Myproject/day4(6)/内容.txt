1.分割了bootpack.c源文件
切割出了dsctbl.c（负责段描述符和中断描述符的初始化和设置）
graphic.c（负责画面显示部分的内容）
bootpack.c（负责汇总和其他杂项）

2.新增了int.c（负责处理外部中断，即中断响应函数）

3.naskfunc.nas增加了中断处理的函数，因为有些指令c语言实现不了，要借助汇编的力量

4.将一些常数搬进了bootpack.h头文件写成宏定义

5.修改了Makefile，增加了一般规则

6.实现了键盘的中断响应